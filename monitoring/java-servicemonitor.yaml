apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor

metadata:
  name: java-backend
  namespace: monitoring

spec:
  namespaceSelector:
    matchNames:
      - java

  selector:
    matchLabels:
      app: backend

  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 30s